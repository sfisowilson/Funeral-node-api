# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=funeral_db

# Base Domain (used for CORS and tenant routing)
BASE_DOMAIN=localhost

# JWT Secret
JWT_SECRET=your-secret-key-here

# Server Port
PORT=3000
